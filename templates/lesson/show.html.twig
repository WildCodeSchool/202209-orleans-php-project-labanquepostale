{% extends 'base.html.twig' %}

{% set title = 'Exercice'  %}

{% block title %}Leçon
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row">
			<h1 class="text-primary text-center mt-4">{{ lesson.title }}</h1>
			<p class="mt-3">{{ lesson.description }}</p>
			<iframe width="450" height="315" src="{{lesson.video}}"></iframe>
		</div>
	</div>
	<div class="mt-2">
		{% include 'Components/_quiz.html.twig' %}
	</div>
{% endblock %}
