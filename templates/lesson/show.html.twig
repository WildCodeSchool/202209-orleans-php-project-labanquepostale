{% extends 'layout.html.twig' %}

{% set title = 'Exercice'  %}
{% set return_path = path('lesson_index', {id: tutorial.id}) %}

{% block title %}Leçon
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row">
			<h1 class="text-primary text-center mt-4">{{ lesson.title }}</h1>
			<p class="mt-3">{{ lesson.description }}</p>
			<div class="text-center my-2">
				<iframe width="315" height="250" src="{{lesson.video}}"></iframe>
			</div>
		</div>
	</div>
	<div class="container my-5">
		{% include 'components/_quiz.html.twig' %}
	</div>
{% endblock %}
