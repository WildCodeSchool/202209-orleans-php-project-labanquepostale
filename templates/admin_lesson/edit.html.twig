{% extends 'layoutAdmin.html.twig' %}

{% block title %}Éditer leçon
{% endblock %}

{% block content %}
	<div class="container text-start">
		<div class="col justify-content-start">
			<div class="row">
				<h1>Éditer leçon : {{ lesson.title }}</h1>
				<div class="gap-2">

				{{ include('admin_lesson/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

				<a href="{{ path('app_admin_tutorial_lesson_show', {'id': lesson.tutorial.id }) }}" class="btn btn-outline-primary mt-2" role="button">Retour à la liste</a>

				{{ include('admin_lesson/_delete_form.html.twig') }}
				</div>
			</div>
		</div>
	</div>
{% endblock %}